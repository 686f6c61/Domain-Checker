# ===== INSTRUCCIONES PARA CONFIGURAR TU ENTORNO =====
# 1. Copia este archivo y renómbralo como .env
# 2. Reemplaza los valores de ejemplo con tus credenciales reales
# 3. No compartas tu archivo .env en sistemas de control de versiones

# == Credenciales de Domains API (RapidAPI) ==
# Regístrate en RapidAPI: https://rapidapi.com/
# Busca la API de Domains: https://rapidapi.com/layered-layered-default/api/domains-api
# Suscríbete a un plan (BASIC es GRATIS - 500 requests/mes)
# Documentación: https://domains-api.com/docs
RAPIDAPI_KEY=tu_clave_api_aqui

# == Variables del servidor ==
# Puerto en el que se ejecuta el servidor (opcional, valor predeterminado: 5000)
PORT=5000

# == CORS Configuration ==
# URL del cliente (para seguridad CORS)
CLIENT_URL=http://localhost:3000

# == NODE Environment ==
# Entorno de ejecución: development, production, test
NODE_ENV=development

# == Rate Limiting ==
# Ventana de tiempo para rate limiting en minutos (por defecto: 15)
RATE_LIMIT_WINDOW_MS=900000
# Número máximo de requests por ventana (por defecto: 100)
RATE_LIMIT_MAX_REQUESTS=100

# == Request Timeout ==
# Timeout para requests a API externa en milisegundos (por defecto: 10000)
API_TIMEOUT=10000
